[workspace]
members = [
    "bcfs",
    "blockchain-traits",
    "mantle-build",
    "mantle",
    "mantle-macros",
    "mantle-test",
    "mantle-types",
    "memchain",
]
exclude = ["examples", "tests"]

[profile.release]
incremental = false
lto = true
opt-level = 3
