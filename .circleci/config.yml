version: 2

jobs:
  init:
    docker:
      - image: nhdh/rust:latest
        environment:
          PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          RUSTC_WRAPPER: sccache
    steps:
      - restore_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
      - run:
          name: Update Rust
          command: rustup update nightly
      - checkout
      - save_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
          paths:
            - ~/.rustup
            - ~/.cargo

  checkstyle:
    docker:
      - image: nhdh/rust:latest
        environment:
          PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          RUSTC_WRAPPER: sccache
    steps:
      - checkout
      - restore_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
      - run:
          name: Checkstyle
          command: cargo fmt -- --check

  unit_test:
    docker:
      - image: nhdh/rust:latest
        environment:
          PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          RUSTC_WRAPPER: sccache
    steps:
      - checkout
      - restore_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
      - run:
          name: Unit tests
          command: cargo test --tests
          environment:
            SKIP_ABI_GEN: 1

  test_xcc:
    docker:
      - image: nhdh/rust:latest
        environment:
          PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          RUSTC_WRAPPER: sccache
    resource_class: xlarge
    steps:
      - checkout
      - restore_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
      - run:
          name: Cross-contract call integration test
          working_directory: tests/xcc-a
          command: cargo test --tests
          environment:
            SKIP_ABI_GEN: 1

  test_ballot:
    docker:
      - image: nhdh/rust:latest
        environment:
          PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
          RUSTC_WRAPPER: sccache
    resource_class: xlarge
    steps:
      - checkout
      - restore_cache:
          key: v1-rust-{{ checksum "~/.rustup/update-hashes/nightly-x86_64-unknown-linux-gnu" }}
      - run:
          name: Ballot Ethereum ABI integration test
          working_directory: tests/ballot
          command: cargo test --tests

workflows:
  version: 2
  test:
    jobs:
      - init
      - checkstyle:
          requires:
            - init
      - unit_test:
          requires:
            - init
      - test_xcc:
          requires:
            - init
      - test_ballot:
          requires:
            - init
