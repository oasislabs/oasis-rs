[workspace]
members = [
    "bcfs",
    "blockchain-traits",
    "idl-gen",
    "memchain",
    "mantle",
    "mantle-macros",
    "mantle-test",
    "mantle-types",
]
exclude = ["examples", "tests"]

[profile.release]
incremental = false
lto = true
opt-level = 3
