[package]
name = "xcc-b"
version = "0.1.0"
authors = ["Oasis Labs <info@oasislabs.com>"]
edition = "2018"
publish = false

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
mantle = { path = "../../mantle" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
mantle-test = { path = "../../mantle-test" }

[build-dependencies]
mantle = { path = "../../mantle" }

[features]
deploy = []
